-- Problem: Find countries with large area or large population
-- Platform: LeetCode
-- Concept: WHERE clause with OR condition

SELECT name, population, area
FROM World
WHERE area >= 3000000 OR population >= 25000000;

-- Explanation:
-- WHERE filters rows based on given conditions
-- OR ensures at least one condition (area or population) is satisfied
